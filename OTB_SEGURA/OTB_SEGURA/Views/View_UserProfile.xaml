<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:OTB_SEGURA.ViewModels"
             xmlns:ImgCircle="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             x:Class="OTB_SEGURA.Views.View_UserProfile"
             Appearing="ContentPage_Appearing"
             x:Name="contentPage">
    <StackLayout>
        <Grid IsClippedToBounds="True">
             <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid IsClippedToBounds="True">
            <ImgCircle:CircleImage HeightRequest="700" BorderColor="Black" BorderThickness="3" Margin="5,10,5,5" Source="{Binding ImgProfile}"></ImgCircle:CircleImage>
            <Button Command="{Binding UploadCommand}" ImageSource="CamaraSubirImg.png" Margin="229,130,120,10" WidthRequest="300" HeightRequest="250" HorizontalOptions="Center" BackgroundColor="{StaticResource PrimaryVariation}" CornerRadius="30" Padding="0"/>
        </Grid>
        

        <Label BindingContext="{Binding User}" Text="{Binding Name,Mode=TwoWay}" HorizontalOptions="Center"/>
        <Label BindingContext="{Binding User}" Text="{Binding Email,Mode=TwoWay}" HorizontalOptions="Center"/>
        <Grid HorizontalOptions="Center" VerticalOptions="Center" Margin="15">
            
            <Button Text="{Binding TextButton,Mode=TwoWay}" Margin="55,5,41,10" WidthRequest="180" HeightRequest="200" Command="{Binding ButtonChangeStateClick}" BackgroundColor="{StaticResource PrimaryVariation}" HorizontalOptions="EndAndExpand"/>
        </Grid>
        
        <Label Text="Alertas"></Label>
    <ListView ItemsSource="{Binding ActivityList}" HasUnevenRows="True" Style="{StaticResource listViewStyle}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Frame Style="{StaticResource frameStyle}">
                        <StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="Fecha: "/>
                                    <Label Text="{Binding Date}" FontAttributes="None"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="Tipo : "/>
                                    <Label Text="{Binding Alert_type_ID}" FontAttributes="None"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="Mensaje: "/>
                                <Label Text="{Binding Message}" FontAttributes="None"/>
                            </StackLayout>
                        </StackLayout>
                    </Frame>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    </StackLayout>
</ContentPage>