<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:OTB_SEGURA.ViewModels"
             xmlns:ImgCircle="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             x:Class="OTB_SEGURA.Views.View_UserProfile"
             Appearing="ContentPage_Appearing"
             x:Name="contentPage">
    <StackLayout>
        <Grid>
             <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid BackgroundColor="{StaticResource PrimaryDisabled}" HeightRequest="450" Grid.ColumnSpan="50"></Grid>
            <ImgCircle:CircleImage WidthRequest="120" HeightRequest="100" BorderColor="Black" BorderThickness="3" Margin="10" Grid.RowSpan="10" Grid.ColumnSpan="50" Source="{Binding ImgProfile}"></ImgCircle:CircleImage>
        </Grid>
       
        
        <Label BindingContext="{Binding User}" Text="{Binding Name,Mode=TwoWay}" HorizontalOptions="Center"/>
        <Label BindingContext="{Binding User}" Text="{Binding Email,Mode=TwoWay}" HorizontalOptions="Center"/>
        <Grid HorizontalOptions="Center" VerticalOptions="Center" Margin="15">
            <Button Command="{Binding UploadCommand}" Text="Subir foto de perfil" Margin="5" WidthRequest="180" HeightRequest="200" HorizontalOptions="Start"/>
            <Button Text="{Binding TextButton,Mode=TwoWay}" Grid.Column="1" Margin="5" WidthRequest="180" HeightRequest="200" Command="{Binding ButtonChangeStateClick}" BackgroundColor="{StaticResource PrimaryVariation}" HorizontalOptions="EndAndExpand"/>
        </Grid>
        
        <Label Text="Alertas"></Label>
    <ListView ItemsSource="{Binding ActivityList}" HasUnevenRows="True" Style="{StaticResource listViewStyle}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Frame Style="{StaticResource frameStyle}">
                        <StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="Fecha: "/>
                                <Label Text="{Binding DateTime}" FontAttributes="None"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="Tipo : "/>
                                <Label Text="{Binding Type}" FontAttributes="None"/>
                            </StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="Mensaje: "/>
                                <Label Text="{Binding Message}" FontAttributes="None"/>
                            </StackLayout>
                        </StackLayout>
                    </Frame>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    </StackLayout>
</ContentPage>